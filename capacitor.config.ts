import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.michaelreiniger.apps.clicker',
  appName: 'Clicker',
  webDir: 'dist/clickerapp/browser'
};

export default config;
